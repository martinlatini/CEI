const submit = document.getElementById("submit");

submit.addEventListener("click", (event) => {
  event.preventDefault();

  const minutos = Number(document.getElementById("mins").value);
  const segundos = Number(document.getElementById("segs").value);
});
